# Approval source boundary

This module is deliberately a capability contract, not a prompt UI. A channel
provider receives immutable call identity and returns one member of the closed
outcome vocabulary. It must honor the request signal. The caller treats throws,
invalid values, and missing providers as unavailable and owns durable audit.
The built-in unavailable and deny providers distinguish missing interaction
capacity from an explicit policy rejection.
