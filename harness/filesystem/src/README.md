# Filesystem source

`index.ts` defines the provider seam, stable errors, result values, and the
session-scoped observation/version policy. Keep host-path handling and tool
schemas outside this package.
