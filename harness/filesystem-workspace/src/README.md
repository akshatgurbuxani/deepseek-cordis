# Workspace filesystem source

`index.ts` contains the confined Node provider, tool schemas, and exact-call
sandbox adapter. Keep policy vocabulary in `@deepseek-cordis/filesystem` and
approval orchestration in `@deepseek-cordis/tools`.
